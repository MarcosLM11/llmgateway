spring:
  application:
    name: llmgateway

gateway:
  providers:
    ollama:
      base-url: http://localhost:11434
    mock:
      enabled: true
      supported-models: mock-fast,mock-slow,qwen2.5-coder:7b
      fail-rate: 0.0
      latency-ms: 5000
